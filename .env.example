# ───── Supabase ─────
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ───── Twilio (SMS OTP) ─────
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
# TWILIO_OTP_FROM_NUMBER=  (not yet set — see docs/INTEGRATIONS.md)

# ───── HelloSign (e-sign) ─────
HELLOSIGN_API_KEY=
# HELLOSIGN_CLIENT_ID=  (not yet set — see docs/INTEGRATIONS.md)
# HELLOSIGN_WEBHOOK_SECRET=  (not yet set — see docs/INTEGRATIONS.md)

# ───── Pelecard (payments — IRON_RULE 2: only allowed provider) ─────
# PELECARD_TERMINAL_NUMBER=  (not yet set — see docs/INTEGRATIONS.md)
# PELECARD_API_USER=  (not yet set — see docs/INTEGRATIONS.md)
# PELECARD_API_PASSWORD=  (not yet set — see docs/INTEGRATIONS.md)
# PELECARD_ENV=test     # test | live  (not yet set — see docs/INTEGRATIONS.md)

# ───── Resend (transactional email) ─────
# RESEND_API_KEY=  (not yet set — see docs/INTEGRATIONS.md)

# ───── Anthropic Claude (AI Import) ─────
ANTHROPIC_API_KEY=

# ───── Cron secrets ─────
# CRON_SECRET=  (not yet set — see docs/INTEGRATIONS.md)
