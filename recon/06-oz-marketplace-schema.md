# 06 — oz-marketplace Schema (this repo)

Sources scanned in this repository (`/Users/daganaar/WebstormProjects/oz-marketplace/`):
- `supabase/migrations/` — **does not exist**
- `supabase/functions/` — **does not exist**
- `supabase/config.toml` — **does not exist**
- `migrations/` (root) — **does not exist**
- Markdown / mdx with SQL fenced blocks — **none** (no markdown files exist either)

## Result

**none found.**

The oz-marketplace repo is bare (see `05-oz-marketplace-state.md`) — there is no `supabase/` directory, no migrations, no SQL files, no schema artifacts of any kind. The schema for the rebuild will be authored fresh, informed by `06-legacy-schema.md` (the legacy baseline).
