# 05 — oz-marketplace Current State

Source: `/Users/daganaar/WebstormProjects/oz-marketplace/`
Captured: 2026-05-02

## Headline finding

The repository is **bare**. There are zero commits, zero application files, zero configs. The only entries beyond `.git/` and `.idea/` are this recon folder and a freshly-generated `.claude/settings.local.json` (created during this recon session). **There is no code, no `package.json`, no Next.js scaffold, no Tailwind, no SCSS.** Everything in this report is therefore "none found" / "not yet present" — the repo is greenfield in the strictest sense.

---

## 6.1 Repo tree (depth 4, excluding standard build dirs)

```
oz-marketplace/
├── .claude/
│   └── settings.local.json
├── .git/                          (bare branch 'main', 0 commits)
├── .idea/                         (JetBrains project metadata)
└── recon/
    └── (this folder — recon outputs only)
```

No `app/`, `src/`, `components/`, `lib/`, `pages/`, `public/`, `styles/`, `supabase/`, `node_modules/`, `.next/`, `dist/`, `build/`, `.turbo/`, `coverage/`, `tests/` — nothing.

Git status:
- Branch: `main`
- Commits: **0** (`fatal: your current branch 'main' does not have any commits yet`)
- Remote tracking branches: none

---

## 6.2 Key configuration files

For every requested file: **none found**.

| File | Status |
|---|---|
| `package.json` | none found |
| `package-lock.json` | none found (cannot list direct deps with versions — there are none) |
| `next.config.*` (any variant: js / mjs / cjs / ts) | none found |
| `tsconfig.json` | none found |
| `.eslintrc.*` (any variant) | none found |
| `.prettierrc.*` (any variant) | none found |
| `.env.example` | none found |
| `README.md` | none found |
| `supabase/config.toml` | none found (no `supabase/` directory) |

---

## 6.3 Styling stack section

| Question | Answer |
|---|---|
| Is `tailwindcss` in `package.json`? what version? | **No** — there is no `package.json` at all. |
| Are `tailwind.config.*` and `postcss.config.*` present? full contents? | **No** — neither file exists. |
| Is `sass` or `sass-embedded` in `package.json`? what version? | **No** — there is no `package.json`. |
| Any `.scss` or `.sass` files in the repo? list every one with size. | **None found.** |
| Any `.css` files? list every one with size. | **None found.** |
| Are there CSS Module files (`.module.css`, `.module.scss`)? list them. | **None found.** |
| Any global stylesheet imports in `app/layout` or `src/app/layout`? quote the import lines. | **No layout files exist.** |

> **Interpretation for the planning phase:** because the repo is bare, the rebuild plan starts from a blank `next create` scaffold. There is no Tailwind to remove, no shadcn to migrate, no existing SCSS to integrate. The brand/ tokens (see 04d) can be ported in directly during scaffolding.

---

## 6.4 Application directory listing

| Path | Contents |
|---|---|
| `app/` | does not exist |
| `src/` | does not exist |
| `components/` | does not exist |
| `lib/` | does not exist |
| `supabase/migrations/` | does not exist |
| `supabase/functions/` | does not exist |

---

## 6.5 shadcn config (`components.json`)

`components.json` does not exist. No shadcn/ui scaffold present. `components/ui/` directory does not exist.

---

## 6.6 Hebrew / RTL setup

No layout file (`app/layout.tsx`, `src/app/layout.tsx`, `pages/_document.tsx`, etc.) exists in the repo.

- `dir="rtl"` — **not set anywhere** (no files to set it on)
- `lang="he"` — **not set anywhere** (no files to set it on)

> The brand system (`brand/design-tokens.md:196-208`, `brand/assets/colors_and_type.css:127`) and `IRON_RULES.md:61-69` (Rule 5: "RTL is non-negotiable") together mandate that the first scaffolded `<html>` element must carry `dir="rtl" lang="he"` and the first global stylesheet must include `body { direction: rtl; }`. This is the baseline before any feature work.

---

## 6.7 `.claude/settings.local.json` (verbatim)

```json
{
  "permissions": {
    "allow": [
      "Bash(grep -rnE '\\\\b[A-Z][A-Z0-9_]{3,}\\\\b' . --include='*.md')",
      "Bash(grep -rnE '^\\\\| `[a-z_]+`' .)",
      "Bash(python3 -c ' *)"
    ]
  }
}
```

Generated by Claude Code during this recon session as a permissions allowlist. Not a project artifact; safe to delete or keep. Not relevant to the rebuild plan.

---

## Summary table

| Aspect | State |
|---|---|
| Code present | None |
| Commits | 0 |
| Tailwind in stack | Not present (and IRON_RULES forbids re-adding it for this rebuild — sccss only per task brief) |
| SCSS / Sass | Not present |
| CSS / CSS Modules | Not present |
| Next.js config | Not present |
| TypeScript config | Not present |
| Supabase scaffold | Not present |
| RTL `dir/lang` | Not set (no layout files) |
| `.env.example` | Not present |
| Lockfile | Not present |
